/* global document */
import header from '../header.html';
import firstPage from '../first-page.html';

document.getElementById('first-page').innerHTML = firstPage;
document.getElementById('header').innerHTML = header;
